int main()
{
	int n = 0;
	scanf_s("%d", &n);
	int ret = 1;
	int i = 1;
	for (i = 1; i <= n; i++)
	{
		ret *= i;
	}
	printf("%d", ret);
	return 0;
}

//函数
int func(int x)
{
	if (x <= 2)
		return x;
	else
		return func(x-1)*x;
		
}
